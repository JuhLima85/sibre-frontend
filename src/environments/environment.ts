export const environment = {
  production: false,
  apiUrlBase: 'http://localhost:8080',  // Backend local
  authBypass: false,        // true = ignora autenticação real (só para teste rápido)
  preferBackend: true,      // usa backend real para autenticação
  devUser: 'dev',
  devPass: '123456',
  devPerfil: 'ADMIN'
};
